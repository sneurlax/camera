name: camera_control_flutter_example
description: "Example app for camera_control_flutter."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  camera_control:
    path: ../../camera_control
  camera_control_flutter:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
