// Relative import to be able to reuse the C sources.
// See the comment in ../<platform>/camera_control_flutter.podspec for more
// information.

// A C source so the FFI plugin has a translation unit; the actual symbols come
// from the cargokit-built Rust static library that is force-linked into the
// framework. This file intentionally has no contents of its own.
