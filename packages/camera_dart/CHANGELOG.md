# Changelog

## 0.0.1

- Initial release. Pure-Dart camera access with a pluggable backend registry.
- Public API: enumerate devices, open a camera, capture frames.
- Backends: native (FFI), web (getUserMedia), stub fallback.
